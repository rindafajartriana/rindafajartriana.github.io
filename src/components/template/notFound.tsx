import _ from "lodash"

const NotFound = ({ routeData }) => {
  return (
    <div className="font-mono text-gray-700">
      <div className="font-semibold uppercase">Route name not found</div>
    </div>
  )
}

export default NotFound