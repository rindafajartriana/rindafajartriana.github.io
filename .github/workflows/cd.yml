# This is a basic workflow to help you get started with Actions

name: React Continous Delivery

# Controls when the action will run. 
on:
  # Triggers the workflow on push or pull request events but only for the master branch
  push:
    branches: [ development ]

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    - name: Deploy using ssh
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.HOST }}
        username: ${{ secrets.USERNAME }}
        # key: ${{ secrets.PRIVATE_KEY }}
        password: ${{ secrets.PASSWD }}
        port: 22
        script: |
          #cd /home/ci_admin/apps/insurance-react
          cd /home/ci_admin/apps
          rm -r insurance-react
          git clone git@github.com-fikry:holisunderground-studio/insurance-react.git
          cd insurance-react
          git checkout development
          #git status
          bash ./prod.sh
        # script: |
          # cd ~/apps/danielwetan/apps/5r
          # git pull origin dev
          # git status
          # docker compose -f ./docker/docker-compose.yml -p 'ksm-api-v1' up -d --build
          # npm install --only=prod
          # pm2 restart node1
